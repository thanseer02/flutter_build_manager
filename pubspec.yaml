name: flutter_build_manager
description: A production-ready open-source Dart CLI package that manages Flutter releases for macOS, Windows, Linux, iOS, and Android.
version: 1.0.16
homepage: https://github.com/thanseer02/flutter_build_manager

environment:
  sdk: ^3.7.0

executables:
  flutter_build_manager:

dependencies:
  args: ^2.5.0
  mason_logger: ^0.3.2
  path: ^1.9.0
  meta: ^1.15.0
  yaml: ^3.1.2
  crypto: ^3.0.3
  intl: ^0.20.2
  convert: ^3.1.2

dev_dependencies:
  lints: ^5.0.0
  mocktail: ^1.0.4
  test: ^1.25.0
